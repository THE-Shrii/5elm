<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Order Confirmation - 5ELM</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #4f46e5; color: white; padding: 20px; text-align: center; }
        .content { padding: 20px; background: #f9f9f9; }
        .order-details { background: white; padding: 20px; margin: 20px 0; border-radius: 8px; }
        .item { display: flex; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid #eee; }
        .total { font-weight: bold; font-size: 18px; color: #4f46e5; }
        .button { background: #4f46e5; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; display: inline-block; }
        .footer { text-align: center; padding: 20px; color: #666; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🛍️ 5ELM E-commerce</h1>
            <h2>Order Confirmation</h2>
        </div>
        <div class="content">
            <h3>Hello {{customerName}}!</h3>
            <p>Thank you for your order! We've received it and it's being processed.</p>
            
            <div class="order-details">
                <h4>Order Details</h4>
                <p><strong>Order Number:</strong> {{orderNumber}}</p>
                <p><strong>Order Date:</strong> {{orderDate}}</p>
                <p><strong>Payment Method:</strong> {{paymentMethod}}</p>
                
                <h4>Items Ordered</h4>
                {{#each items}}
                <div class="item">
                    <div><strong>{{name}}</strong><br>Quantity: {{quantity}}</div>
                    <div>₹{{subtotal}}</div>
                </div>
                {{/each}}
                
                <div class="item total">
                    <div>Total Amount</div>
                    <div>₹{{total}}</div>
                </div>
            </div>
            
            <div style="text-align: center;">
                <a href="{{trackingUrl}}" class="button">Track Your Order</a>
            </div>
        </div>
        <div class="footer">
            <p>Thank you for shopping with 5ELM!</p>
            <p>📧 support@5elm.com</p>
        </div>
    </div>
</body>
</html>